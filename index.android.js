// Boilerplate to render main.js in iOS
const React = require('react-native')
const { AppRegistry } = React
const Main = require('./src/main')


AppRegistry.registerComponent('authenticationblah', () => Main)